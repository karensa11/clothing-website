import shopData from "../../data/shop-items.data";
const INITIAL_STATE = {
    shopItems: shopData
};

const reducer = (state = INITIAL_STATE, action) => {
    return state;
};

export default reducer;
